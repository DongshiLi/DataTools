C#读取WIND数据库数据并并表
逻辑如下：
1.根据股票代码和交易时间为index做一个map
2.通过map填充已有wind数据源
3.如果map中缺少某一只股票的某一天数据，preclose为前一个交易日close，其余高开低收价格等于当日preclose，成交额成交量为0
4.缺少的数据置为0，并根据当日五个指数，并行填充五个指数的价格及股票权重，如此股票未在此指数中，权重置为0
5.特殊处理及上退市根据原数据来进行处理，0/1代表特殊处理及上退市状态
